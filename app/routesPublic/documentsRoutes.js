// import DocumentController from '../controllers/documentsController';

// export default (router, app) => {
//   const modelBase = 'documents';
  
//   const documentController = new DocumentController(app, modelBase);

//   router.get(`/${modelBase}`, documentController.getDocumentById.bind(documentController));
//   router.get(`/${modelBase}/:entityName/:entityId`, documentController.getDocumentByEntityNameAndId.bind(documentController));

// };
